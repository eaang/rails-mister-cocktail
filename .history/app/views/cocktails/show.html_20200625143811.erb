<div class="container">
  <div class="col-12">
    <h1><%= @cocktail.name.split.map(&:capitalize)*' ' %></h1>
    <ul>
      <% @doses.each do |dose| %>
        <li>
        <%= "#{dose.description} #{dose.ingredient.name.split.map(&:capitalize)*' '}" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>

