<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>Cocktail Database</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="bg-primary text-light">
    <nav id="navbar" class="navbar mh-7em navbar-expand-md sticky-top navbar-light bg-light">
      <%= link_to root_path, class: 'navbar-brand' do %>
        <i class="h1 fas fa-cocktail"></i>
      <% end %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <div class="end-wrapper">
        <div class="side-wrapper d-flex flex-wrap justify-content-end p-0 col-12">
          <%= form_tag(search_path, method: "get", class: 'form-row flex-fill col-12 col-md-3 p-0') do %>
            <div class="input-group">
              <%= search_field_tag :search, params[:search], placeholder: "Search name or ingredient", class: "form-control bg-dark text-light" %>
              <div class="input-group-btn">
                <%= button_tag class: 'btn btn-primary btn-block' do %>
                  <i class="fas fa-search"></i>
                <% end %>
              </div>
            </div>
          <% end %>
          <div class="flex-fill btn-group col-12 col-md-2 p-0" role="group" aria-label="Button group with nested dropdown">
            <div class="flex-fill btn-group" role="group">
              <button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Create New Cocktail </button>
              <div class="dropdown-menu dropdown-menu-right bg-primary" aria-labelledby="btnGroupDrop1" style="">
                <%= simple_form_for Cocktail.new, html: { class: 'px-2 py-1' } do |f| %>
                  <%= f.input :name, label: 'Cocktail Name', placeholder: 'Name', input_html: { class: 'bg-dark text-light' } %>
                  <%= f.input :direction, as: :text, label:'Directions', placeholder: 'Directions', input_html: { class: 'bg-dark text-light' } %>
                  <%= f.input :photo, label: 'Upload an Image', as: :file %>
                  <%= f.button :submit, 'Create', class: 'btn btn-dark btn-block' %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
      </div>
    </nav>
    <%= yield %>
    <div id="sizing-box" style="height:0;width:0;outline:none;border:none;padding:none;margin:none;box-sizing:content-box;"></div>
  </body>
</html>
